{
  "name": "open-car-setup",
  "version": "1.0.0",
  "description": "Setup scripts for OpenCar dashboard",
  "private": true,
  "scripts": {
    "install-deps": "tsx stc/install-dependencies.ts",
    "setup-can": "tsx src/setup-can.ts",
    "setup-system": "tsx src/setup-system.ts",
    "setup-all": "npm run install-deps && npm run setup-can && npm run setup-system"
  },
  "dependencies": {
    "chalk": "^4.1.2",
    "commander": "^11.1.0",
    "execa": "^5.1.1",
    "fs-extra": "^11.2.0",
    "ora": "^5.4.1",
    "tsx": "^4.19.3"
  },
  "devDependencies": {
    "@types/fs-extra": "^11.0.4",
    "@types/node": "^20.10.5",
    "typescript": "^5.3.3",
    "vite": "^5.0.10"
  }
}
